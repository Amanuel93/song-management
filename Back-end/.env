# MongoDB connection URI
MONGODB_URI=mongodb+srv://horamallee193:Aman12345@cluster0.tv84e.mongodb.net/song-management?ssl=true&replicaSet=atlas-116o4h-shard-0&authSource=admin&retryWrites=true&w=majority&appName=Cluster0

# Server port
PORT = 5000

# CORS configuration (Optional)
# You can specify which domains are allowed to access your API
# CORS_ORIGIN=http://your-frontend-domain.com

# Multer file upload size limit (Optional)
# This is just for reference, not typically set in .env but could be useful
# MULTER_FILE_SIZE_LIMIT=1000000  # 1MB

# JWT Secret for token generation (if needed in future)
# JWT_SECRET=your_jwt_secret_key

# EMAIL_PASS=your_email_password
# Email service credentials (if needed in future)
# EMAIL_USER=your_email@example.com